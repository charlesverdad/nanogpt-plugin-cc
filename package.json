{
  "name": "nanogpt-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hand off tasks and code reviews from Claude Code to NanoGPT subscription models.",
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "bump-version": "node scripts/bump-version.mjs",
    "check-version": "node scripts/bump-version.mjs --check",
    "test": "node --test tests/*.test.mjs"
  }
}
